spring:
  datasource:
    url: jdbc:h2:mem:testdb
  flyway:
      enabled: true
      locations: classpath:db/migration/shema,classpath:db/testdata
  output:
    ansi:
      enabled: always
app:
  version: 0.0.1